import { CaseItem } from '../models/caseItemModel';
import { Case } from '../models/caseModel';

export function performSpinHelper(
  clientSeed: string,
  serverSeed: string,
  caseData: Case //   Change this to the case data type
): CaseItem {
  return null;
}
