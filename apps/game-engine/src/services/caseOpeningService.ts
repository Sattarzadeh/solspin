// import { CaseItem } from "../models/case_item_model";
// import { determineItem } from "./caseItemDeterminationService";
// import { generateRollValue } from "./caseItemDeterminationService";
// import { mockCase } from "../mock/case_pot_of_gold.mock";

// export const demoSpin = async (caseName: string): Promise<CaseItem> => {
//   return performSpin(caseName);
// };

// export const normalSpin = async (caseName: string, token: string): Promise<CaseItem> => {
//   // Placeholder for normal spin logic, including validation of the token
//   return performSpin(caseName);
// };

// const performSpin = (caseName: string): CaseItem => {
//   const rollValue = generateRollValue();
//   const rewardItem = determineItem(rollValue, mockCase);
//   return rewardItem;
// };
